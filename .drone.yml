image: ubuntu:14.04
env:
  - GOPATH=/var/cache/drone
  - DEBUG=true
script:
  - wget http://gitlab.juancastro.org/juan/randomaas/raw/master/Dockerfile
  - docker build -t --rm Dockerfile Randomaas
#services:
#  - dockerfile/
notify:
  email:
    recipients:
      - krazybean1@yahoo.com
      - krazybean1@yahoo.com
